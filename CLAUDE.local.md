# CLAUDE.local.md — shaxsiy override'lar (gitignored)

> Bu fayl faqat **sizning** mashinangiz uchun. Git'ga tushmaydi.
> Komandaviy umumiy qoidalar — `CLAUDE.md` da.

## Mening lokal sozlamalarim

- OS: Windows 11, PowerShell.
- Backend DB: PostgreSQL `workplus` @ `localhost:5432`. Parol va boshqa env — `Backend/.env`.
- Backend port: 8000 · Frontend port: 5173.

## Lokal env eslatmalar

- Maxfiy qiymatlar (`DB_PASSWORD`, `JWT_SECRET`, API kalitlar) **faqat** `Backend/.env` va
  `.claude/settings.local.json` ichida. Bularni hech qachon `CLAUDE.md` yoki `settings.json` ga ko'chirмa.
- Claude Code modeli/proxy konfiguratsiyasi `.claude/settings.local.json` da (shaxsiy).

## Shaxsiy ish-eslatmalari (scratch)

<!-- Hozir nima ustida ishlayotганingizni shu yerda yozib turing — Claude shu kontekstни ko'radi. -->

- [ ] ...
